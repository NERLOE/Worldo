import { StyleSheet } from "react-native";

export const globalStyles = StyleSheet.create({
  container: {
    flex: 1,
    padding: 10,
    backgroundColor: "#000",
  },
  titleText: {
    color: "#fff",
    fontSize: 24,
  },
  descriptionText: {
    color: "#fff",
    fontSize: 12,
  },
});
